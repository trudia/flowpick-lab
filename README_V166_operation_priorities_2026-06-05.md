# v166 Operation Priorities Update

## Current Check

- Live site baseline is `v165-centered-home-slider-2026-06-04`.
- Homepage, sitemap, creator side hustle category, and the two recent shorts-related posts return HTTP 200.
- The Google Form CTA is present on `shorts-side-hustle-gear-before-start`.
- Local worktree is clean except for the existing untracked `.git2/` directory.

## Improvements Applied

- Rebuilt `BLOG_OPERATION_AUTOMATION_PROCESS.md` as a clean current operating playbook.
- Rebuilt `SHOPPING_PUBLISHING_PROCESS.md` with the current v165 baseline, v166 next-patch rule, Coupang Partners API boundaries, affiliate notice, and search submission rules.
- Rewrote daily, weekly, and monthly operation templates in readable Korean.
- Added the 6-hour accelerated operating loop to the working templates: 03:00, 09:00, 15:00, 21:00 KST.

## Current Priorities

1. Keep the homepage stable for now. The v165 slider and centered copy are working, so the next improvements should focus on content depth and routing instead of another visual redesign.
2. Strengthen the creator side hustle cluster. The current route should connect `shorts-topic-test-7day-routine` and `shorts-side-hustle-gear-before-start` more clearly with next-step criteria.
3. Recheck Google Search Console indexing after quota recovery. Sitemap and IndexNow automation should continue, but normal blog post manual indexing still depends on Search Console quota.
4. Refresh existing posts before adding too many new ones. Priority goes to posts with weak next-step links, repeated images, or unclear category placement.
5. Use shopping posts only when product intent is clear. Otherwise, keep the content as information-first guidance or a form-based diagnosis.

## Next Actions

- Update the active automation prompt to use the 6-hour checkpoint model.
- At the next 09:00 deep checkpoint, decide whether to publish a new creator-side-hustle follow-up or refresh the two existing shorts posts first.
- At the next 21:00 deep checkpoint, review completed work, indexing state, and the next 6-hour action.
